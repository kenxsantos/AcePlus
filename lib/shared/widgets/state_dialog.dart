import 'package:flutter/material.dart';

class StateDialog extends StatelessWidget {
  const StateDialog({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
